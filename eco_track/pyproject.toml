[project]
name = "eco_track"
authors = [{name = "Developer"}]
description = "ESG and Carbon Footprint Tracking"
requires-python = ">=3.10"
version = "0.0.1"

[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"
